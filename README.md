# Student Grade Analyzer

This Java project allows users to input student names and scores, convert scores to letter grades, and display a class average.

## Key Features
- Uses `Scanner` for user input
- Uses `arrays`, `loops`, and `methods`
- Calculates letter grades and class average

## How to Run
Compile and run `StudentGradeAnalyzer.java` in any Java IDE or command line.
